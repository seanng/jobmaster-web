/*
 * CreatePostButton Messages
 *
 * This contains all the text for the CreatePostButton component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  header: {
    id: 'app.components.CreatePostButton.header',
    defaultMessage: 'Create New Post',
  },
});
