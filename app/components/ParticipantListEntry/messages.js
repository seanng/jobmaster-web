/*
 * ParticipantListEntry Messages
 *
 * This contains all the text for the ParticipantListEntry component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  header: {
    id: 'app.components.ParticipantListEntry.header',
    defaultMessage: 'This is the ParticipantListEntry component !',
  },
});
