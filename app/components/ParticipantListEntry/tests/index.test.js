// import React from 'react';
// import { shallow } from 'enzyme';

// import ParticipantListEntry from '../index';

describe('<ParticipantListEntry />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
