// import { fromJS } from 'immutable';
// import { makeSelectHelpDomain } from '../selectors';

// const selector = makeSelectHelpDomain();

describe('makeSelectHelpDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
