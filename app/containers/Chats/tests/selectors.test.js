// import { fromJS } from 'immutable';
// import { makeSelectChatsDomain } from '../selectors';

// const selector = makeSelectChatsDomain();

describe('makeSelectChatsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
