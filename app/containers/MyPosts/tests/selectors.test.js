// import { fromJS } from 'immutable';
// import { makeSelectMyPostsDomain } from '../selectors';

// const selector = makeSelectMyPostsDomain();

describe('makeSelectMyPostsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
