// import { fromJS } from 'immutable';
// import { makeSelectCreatePostDomain } from '../selectors';

// const selector = makeSelectCreatePostDomain();

describe('makeSelectCreatePostDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
